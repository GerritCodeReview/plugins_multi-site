[remote "Replication"]
    $REPLICATION_URL
    push = +refs/*:refs/*
    timeout = 600
    rescheduleDelay = 15
    replicationDelay = 5
[gerrit]
    autoReload = true
    replicateOnStartup = true
[replication]
    lockErrorMaxRetries = 5
    maxRetries = 5