[healthcheck]
  timeout = 10s

[healthcheck "querychanges"]
  query = status:open OR status:merged OR status:abandoned
  # skipping this check since there are no changes.
  limit = 0

[healthcheck "auth"]
  userame = "admin"
